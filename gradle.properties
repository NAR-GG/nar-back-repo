# Gradle 빌드 캐시 — 입력이 같은 태스크의 산출물을 재사용한다.
# CI에서는 actions/cache 로 ~/.gradle/caches 가 복원되므로 clean build 도 미변경 태스크를 건너뛴다.
org.gradle.caching=true

# 태스크 병렬 실행. CI 러너가 4코어인데 순차로만 돌고 있었다.
org.gradle.parallel=true
